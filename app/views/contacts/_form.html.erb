<% remote ||= false %>
<% dom_id = if  @contact.new_record?
	"new_service"
else
	dom_id(@contact)
end
%>

<%= redpill_tabular_form(@contact,{:object_name => 'contact'},{:remote => remote}) do |f| %> 
	<%= f.text_field :name %>	
 <%= f.text_field :email %>	 
	
	<%= f.submit_and_cancel_buttons %>
<% end %>
