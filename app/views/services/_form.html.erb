<% remote ||= false %>
<% dom_id = if  @service.new_record?
	"new_service"
else
	dom_id(@service)
end
%>

<%= redpill_tabular_form(@service,{:object_name => 'service'},{:remote => remote}) do |f| %> 
	<%= f.text_field :name %>	
	<%= f.empty_column  %>
	
	<%= f.submit_and_cancel_buttons %>
<% end %>
