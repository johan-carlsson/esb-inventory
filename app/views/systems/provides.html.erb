<% provides||=@provides %>
<% pages||=PaginationHelper.pages(provides,params) %>
<%= render :partial => "system" %>

<%= render :partial => "tabs" %>
<div id="result">
		<div id="table">
      <%= render :partial => "shared/table_toolbar", :locals => {:name => "provides", :collection => provides, :pages => pages} %>
      <%= render :partial => "provides_table", :locals => {:provides => pages.current_page} %>
		</div>
</div>
