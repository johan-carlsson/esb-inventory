<% roles||=@roles %>
<% pages||=PaginationHelper.pages(roles,params) %>
<%= render :partial => "system" %>

<%= render :partial => "tabs" %>
<div id="result">
		<div id="table">
      <%= render :partial => "shared/table_toolbar", :locals => {:name => "contacts", :collection => roles, :pages => pages} %>
      <%= render :partial => "roles/table", :locals => {:roles => pages.current_page} %>
		</div>
</div>
