<% remote ||= false %>
<% dom_id = if  @consumer.new_record?
	"new_service"
else
	dom_id(@consumer)
end
%>

<%= redpill_tabular_form(@consumer,{:object_name => 'consumer'},{:remote => remote}) do |f| %> 
	<%= f.text_field :name %>	
 <%= f.text_field :identifier %>	 
	
	<%= f.submit_and_cancel_buttons %>
<% end %>
